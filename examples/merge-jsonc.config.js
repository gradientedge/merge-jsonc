// Example merge-jsonc configuration file
export default {
  // Default output file
  out: "dist/config.json",

  // Skip missing input files by default
  skipMissing: true,

  // Pretty print with 2-space indentation
  pretty: true,
  indent: 2,

  // Create backups when overwriting
  backup: true,

  // Dry run mode (preview only)
  dryRun: false,
};
